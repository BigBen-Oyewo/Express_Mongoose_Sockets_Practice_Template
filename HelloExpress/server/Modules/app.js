// There are also multiple ways we can Import our functions. Save our exports in a variable:

const myCustomModule = require("./my_module");
myCustomModule.greet();
myCustomModule.add(5, 7);



// Or we can use destructuring like this below:

// const { greet, add } = require("./my_module");
// greet();
// add(5, 7);


